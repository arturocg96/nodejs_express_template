//const db = require('../db/connectionSQL');

/**
 * Función ejemplo
 * const saveEjemplo = async (data) => {
 * try {
 * } catch (error) {
 * throw new Error(error.message);
 * }
 * };
 * 
 * module.exports = {  
 * saveEjemplo
 * };
 */

/**const ejemploModel = require('../models/ejemploModel');
 * 
 * const ejemploController = async (req, res) => {
 * try {
 * } catch (error) {
 * res.status(500).json({ message: error.message });
 * }
 * };
 * 
 * module.exports = { 
 * ejemploController
 * };
 */